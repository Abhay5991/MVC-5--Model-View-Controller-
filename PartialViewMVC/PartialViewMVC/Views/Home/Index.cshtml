@model  IEnumerable<PartialViewMVC.Models.Employee>
@{
    ViewBag.Title = "Index";
    
}

@foreach (var item in Model)
{
    Html.RenderPartial("_Card" , item);
}

@*@for (int i = 0; i < n; i++)
{
    @Html.Partial("_Card")
}*@

@*@{Html.RenderPartial("_Card"); }*@